node_modules/
dist/
*.tsbuildinfo
build/
.env
.env.production
.env.*.local
.DS_Store
*.log
.turbo/
.copilot-reviews/
/screenshots/

# Playwright (story 67)
test-results/
playwright-report/

# Generated UX review artefacts (ux-tests/README.md)
ux-tests/reports/*
!ux-tests/reports/.gitkeep

# Playwright MCP scratch (snapshots/console logs from agent exploration).
# Written next to the cwd the MCP server runs in, which is the repo root, not
# the workspace the config lives in — an earlier ignore rule guessed the latter
# and duly committed 20-odd scratch files.
.playwright-mcp/
