# Pin the public npm registry for this project, overriding any
# machine-level ~/.npmrc registry override (e.g. a corporate Nexus
# mirror). This keeps package-lock.json portable for every contributor —
# see docs/dependencies.md "Confirmed deviations" for why this exists.
registry=https://registry.npmjs.org/
