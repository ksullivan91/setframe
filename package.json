{
  "name": "setframe",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10.9.7",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "check:deps": "node scripts/check-peer-deps.mjs",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "eslint-plugin-react-hooks": "^7.1.1",
    "turbo": "2.10.11",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.69.0"
  },
  "engines": {
    "node": ">=22"
  }
}
