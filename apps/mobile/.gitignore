.expo/
dist/
web-build/

# Native projects are generated by `expo prebuild` (Continuous Native
# Generation) from app.json + the config plugins listed there — app.json is
# the source of truth, not these directories. Regenerate with
# `npx expo prebuild --clean` rather than editing them by hand or
# committing them; anything hand-edited here is silently discarded on the
# next prebuild.
/ios/
/android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.env
.env.*.local

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli