import { ExerciseHistoryScreen } from '../../../../src/screens/ExerciseHistoryScreen';

/** One exercise's history, inside Progress so the tab bar stays. */
export default function ExerciseHistoryRoute() {
  return <ExerciseHistoryScreen />;
}
