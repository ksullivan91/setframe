import { PlansScreen } from '../../../src/screens/PlansScreen';

/** "Your plans" (story 81), reached from the overview's Change. */
export default function PlansRoute() {
  return <PlansScreen />;
}
