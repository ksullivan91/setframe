{
  "mcpServers": {
    "playwright-test": {
      "command": "npx",
      "args": [
        "playwright",
        "run-test-mcp-server",
        "--config",
        "apps/web/playwright.config.ts",
        "--headless"
      ]
    }
  }
}
